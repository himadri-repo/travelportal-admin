import { ReplaceStringPipePipe } from './replace-string-pipe.pipe';

describe('ReplaceStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
